//This was going to be a way of abstracting out forms, props, etc.,
//but it doesn't work the way I wanted.

import React from 'react'

const Formats=({type, thing})=> {
  

if (this.type === 'dog'){
 return (   <div>Name: {thing.petName}<br/>
            Breed: {thing.petBreed}<br/>
            Age: {thing.petAge}<br/>
            Description: {thing.petDescription}<br/>
            </div>
    )}
}
export default Formats;